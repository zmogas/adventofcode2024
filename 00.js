const fs = require("fs");

const day = "00";

const input = fs.readFileSync(`${day}.txt`, "utf-8");

// console.log({ input });
const lines = input.split("\n");
// console.log(lines.length, { lines });
